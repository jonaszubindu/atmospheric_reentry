# Local virtual environment (not for pushing)
.venv/

# Python bytecode / caches
__pycache__/
*.py[cod]
.ruff_cache/

# Packaging / build artifacts
*.egg-info/
build/
dist/

# cfgrib index sidecar files generated next to .grib data
*.grib.*.idx

# Secrets — API keys etc. (openAIP key for the basemap)
.env
